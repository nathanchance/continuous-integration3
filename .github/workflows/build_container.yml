name: build and push runner container images
on:
  pull_request:
    paths:
      - .github/workflows/build_container.yml
      - runner/env
      - runner/build_container_image.sh
      - runner/push_container_image.sh
  push:
    paths:
      - .github/workflows/build_container.yml
      - runner/env
      - runner/build_container_image.sh
      - runner/push_container_image.sh
  schedule:
    - cron: 0 0 * * 0
  workflow_dispatch:
    inputs:
      push_image:
        description: 'Push image to ghcr.io'
        required: false
        type: boolean

jobs:
  build_and_push_kernel_container_image:
    name: Build and push runner container images
    permissions:
      contents: read
      packages: write
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        type: [kernel, llvm]
    steps:
      - name: Clone repository
        uses: actions/checkout@v7

      - name: Setup mkosi
        uses: systemd/mkosi@main

      - name: Build ${{ matrix.type }} container image
        run: runner/build_container_image.sh ${{ matrix.type }}

      - name: Push ${{ matrix.type }} container image
        if: ${{ github.repository_owner == 'nathanchance' && ((github.event_name == 'workflow_dispatch' && github.event.inputs.push_image == 'true') || github.event_name == 'schedule') }}
        run: runner/push_container_image.sh ${{ matrix.type }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
